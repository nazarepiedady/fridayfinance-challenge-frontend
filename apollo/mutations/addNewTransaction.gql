mutation addNewTransaction($categoryName: String!, $transactionId: String!) {
  addNewTransaction(
    categoryName: $categoryName
    transactionId: $transactionId
  ) {
    id
    date
    amount
    currency
    reference

    category {
      name
      color
    }
  }
}
